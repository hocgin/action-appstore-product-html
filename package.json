{
  "name": "action-appstore-product-html",
  "version": "1.0.0",
  "description": "GitHub Action to fetch an App Store developer catalog and output HTML.",
  "private": true,
  "license": "MIT",
  "type": "commonjs",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "lint": "node --check src/index.js",
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
